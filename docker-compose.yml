services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      DATABASE_URL: "file:/app/data/app.db"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
